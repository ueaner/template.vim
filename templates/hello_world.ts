// define the class with constructor
class Main {
    constructor (public Name: string) { }
}

// create the instance of the class
let m = new Main("Hello, World!");
​
// print the information in the console
console.log(`${m.Name}`);
